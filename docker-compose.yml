services:
  app:
    build: .
    ports:
      - "7066:7066"
    env_file:
      - .env
    volumes:
      - ./sims_mods.db:/app/sims_mods.db
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - app
    restart: unless-stopped