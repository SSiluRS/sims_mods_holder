# Sims Mods Holder

Веб-приложение для управления и каталогизации модов для The Sims. Позволяет добавлять ссылки на моды, автоматически парсить информацию (заголовок, картинка) и тегировать их.

## 🛠 Технологический стек

- **Backend**: Python 3.11, Flask, SQLite / MySQL
- **Frontend**: Vue.js 3, Vite, Tailwind CSS (или custom CSS)
- **Containerization**: Docker, Docker Compose
- **Server**: Nginx (внутри Docker контейнера фронтенда)

## 🚀 Быстрый старт (Docker)

Это рекомендуемый способ запуска приложения.

1. **Клонируйте репозиторий**
2. **Создайте файл .env** (необязательно для SQLite, используется по умолчанию)
3. **Запустите через Docker Compose**:

```bash
docker-compose up --build
```

После запуска приложение будет доступно по адресам:
- **Frontend (UI)**: [http://localhost:8080](http://localhost:8080)
- **Backend (API)**: [http://localhost:7066](http://localhost:7066)

## 💻 Ручной запуск (для разработки)

### Бэкенд

1. Перейдите в корень проекта.
2. Создайте и активируйте виртуальное окружение:
   ```bash
   python -m venv .venv
   # Windows
   .\.venv\Scripts\activate
   # Linux/macOS
   source .venv/bin/activate
   ```
3. Установите зависимости:
   ```bash
   pip install -r backend/requirements.txt
   ```
4. Запустите сервер:
   ```bash
   python backend/run.py
   ```
   Сервер запустится на `http://localhost:7066`.

### Фронтенд

1. Перейдите в папку `frontend`:
   ```bash
   cd frontend
   ```
2. Установите зависимости:
   ```bash
   npm install
   ```
3. Запустите режим разработки:
   ```bash
   npm run dev
   ```
   Приложение будет доступно на `http://localhost:5173`.

## 📁 Структура проекта

```
sims_mods_holder/
├── backend/            # Flask приложение
│   ├── app/            # Логика приложения (API, БД, утилиты)
│   ├── run.py          # Точка входа
│   └── requirements.txt
├── frontend/           # Vue.js приложение
│   ├── src/            # Исходный код (компоненты, роутер)
│   ├── Dockerfile      # Сборка фронтенда + Nginx
│   └── nginx.conf      # Конфиг Nginx для фронтенда
├── docker-compose.yml  # Оркестрация контейнеров
└── ...
```

## ⚙️ Конфигурация

Настройки бэкенда находятся в `backend/config.py`. Вы можете переопределять их через переменные окружения.

Основные переменные:
- `FLASK_ENV`: development / production
- `DATABASE_TYPE`: sqlite (по умолчанию) или mysql
- `SECRET_KEY`: Секретный ключ приложения